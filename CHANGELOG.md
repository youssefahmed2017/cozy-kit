# CHANGELOG
## v1.0.1
- Expanded ```bash
cozy-kit --info```
with more details

- Added `project.urls` to `pyproject.toml`
- Added keywords to `pyproject.toml`
- Expanded classifiers
- Added maintainers to `pyproject.toml`
- Changed `README.md` with more badges
- Added parameter `message` to `CozyUI.spinner`
- Made `CozyUI.progress_bar` cleaner
- Added instant thread support to `start_pomodoro`
- Renamed `pomodoro` to `start_pomodoro`
- Added the cozy-kit logo to `README.md`
  
## v1.0.3
### Bug Fixes
- Fixed the `ImportError`

### Features
- Made `--version` also have `-V`
- Addeed `--license` to the CLI
- Addeed `--pypi` to the CLI
- Addeed `--github` to the CLI
- Addeed `--homepage` to the CLI
- Addeed `--docs` to the CLI
